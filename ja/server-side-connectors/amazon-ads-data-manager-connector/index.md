---
title: Amazon Ads Data Manager コネクタ構成ガイド
description: この記事では、Amazon Ads Data Manager コネクタの構成方法について説明します。
url: https://docs.tealium.com/ja/server-side-connectors/amazon-ads-data-manager-connector/
---
## バッチ制限

このコネクタは、ベンダーへの大量データ転送をサポートするためにバッチリクエストを使用します。詳細については、[バッチアクション](https://docs.tealium.com/batched-actions/)を参照してください。リクエストは、次のいずれかの閾値に達するか、プロファイルが公開されるまでキューに入れられます：

* リクエストの最大数：10,000
* 最古のリクエストからの最大時間：60分
* リクエストの最大サイズ：2 MB

## 構成

コネクタマーケットプレイスにアクセスし、新しいコネクタを追加します。コネクタを追加する一般的な手順については、[コネクタについて](https://docs.tealium.com/about-connectors/)を参照してください。

コネクタを追加した後、以下の構成を構成します：
* **API Base URL**  
  * API Base URLを選択します。
* **Amazon Ads Data Manager ID**  
  * あなたのAdsデータマネージャー（ADM）マネージャーID。これを見つけるには、ADM UIの右上メニューでマネージャーアカウントをクリックします。

## アクション

| アクション名 | AudienceStream | EventStream |
| --- | :---: | :---: |
| オーディエンスに訪問を追加 | ✓ | ✓ |
| オーディエンスから訪問を削除 | ✓ | ✓ |

アクションの名前を入力し、ドロップダウンメニューからアクションタイプを選択します。

次のセクションでは、各アクションのパラメータとオプションの構成方法について説明します。

### オーディエンスに訪問を追加

#### パラメータ

| **パラメータ** | **説明** |
| --- | --- |
| Audience Dataset | ADMオーディエンスデータセットを選択します。ADMオーディエンスデータセットを作成するには、**Create Audience Dataset**をクリックします。 |

#### 外部ユーザーID

| **パラメータ** | **説明** |
| --- | --- |
| External ID (apply SHA256 hash) | コネクタがAmazonに送信する前に外部ID値をハッシュ化する場合は、このパラメータを使用します。 |
| External ID (already SHA256 hashed) | すでに空白をトリムし、小文字に変換し、SHA256でハッシュ化された外部IDを提供します。 |

#### ユーザー識別子

| **パラメータ** | **説明** |
| --- | --- |
| Email Address (already SHA256 hashed) | すでに空白をトリムし、小文字に変換し、SHA256でハッシュ化されたメールアドレスを提供します。 |
| Email Address (apply SHA256 hash) | プレーンテキストのメールアドレスを提供し、コネクタがこの値を空白トリムし、小文字に変換し、SHA256ハッシュを使用してハッシュ化します。 |
| Phone Number (already SHA256 hashed) | すでに空白をトリムし、小文字に変換し、SHA256でハッシュ化された電話番号を提供します。 |
| Phone Number (apply SHA256 hash) | プレーンテキストの電話番号を提供し、コネクタがこの値を空白トリムし、小文字に変換し、SHA256ハッシュを使用してハッシュ化します。 |
| First Name (already SHA256 hashed) | すでに空白をトリムし、小文字に変換し、SHA256でハッシュ化された名前を提供します。 |
| First Name (apply SHA256 hash) | プレーンテキストの名前を提供し、コネクタがこの値を空白トリムし、小文字に変換し、SHA256ハッシュを使用してハッシュ化します。 |
| Last Name (already SHA256 hashed) | すでに空白をトリムし、小文字に変換し、SHA256でハッシュ化された姓を提供します。 |
| Last Name (apply SHA256 hash) | プレーンテキストの姓を提供し、コネクタがこの値を空白トリムし、小文字に変換し、SHA256ハッシュを使用してハッシュ化します。 |
| Address (already SHA256 hashed) | すでに空白をトリムし、小文字に変換し、SHA256でハッシュ化された住所を提供します。 |
| Address (apply SHA256 hash) | プレーンテキストの住所を提供し、コネクタがこの値を空白トリムし、小文字に変換し、SHA256ハッシュを使用してハッシュ化します。 |
| City (already SHA256 hashed) | すでに空白をトリムし、小文字に変換し、SHA256でハッシュ化された市を提供します。 |
| City (apply SHA256 hash) | プレーンテキストの市を提供し、コネクタがこの値を空白トリムし、小文字に変換し、SHA256ハッシュを使用してハッシュ化します。 |
| State (already SHA256 hashed) | すでに空白をトリムし、小文字に変換し、SHA256でハッシュ化された州を提供します。 |
| State (apply SHA256 hash) | プレーンテキストの州を提供し、コネクタがこの値を空白トリムし、小文字に変換し、SHA256ハッシュを使用してハッシュ化します。 |
| Postal Code (already SHA256 hashed) | すでに空白をトリムし、小文字に変換し、SHA256でハッシュ化された郵便番号を提供します。 |
| Postal Code (apply SHA256 hash) | プレーンテキストの郵便番号を提供し、コネクタがこの値を空白トリムし、小文字に変換し、SHA256ハッシュを使用してハッシュ化します。 |
| MAID | モバイル広告ID。 |
| LiveRamp ID | LiveRamp ID。 |

#### 国コード

| **パラメータ** | **説明** |
| --- | --- |
| Country Code | 国コードは、ISO 3166形式の2文字の文字列で、データが収集された国を示します。レコードレベルで追加された国コードは、オーディエンスレベルで追加された国コードを上書きします。 |
| IP Address | IPアドレスは、このオーディエンスに追加されたメンバーの国を決定するために使用されます。 |

### オーディエンスから訪問を削除

#### パラメータ

| **パラメータ** | **説明** |
| --- | --- |
| Audience Dataset | ADMオーディエンスデータセットを選択します。 |

#### 外部ユーザーID

| **パラメータ** | **説明** |
| --- | --- |
| External ID (apply SHA256 hash) | コネクタがAmazonに送信する前に外部ID値をハッシュ化する場合は、このパラメータを使用します。 |
| External ID (already SHA256 hashed) | すでに空白をトリムし、小文字に変換し、SHA256でハッシュ化された外部IDを提供します。 |

#### ユーザー識別子

| **パラメータ** | **説明** |
| --- | --- |
| Email Address (already SHA256 hashed) | すでに空白をトリムし、小文字に変換し、SHA256でハッシュ化されたメールアドレスを提供します。 |
| Email Address (apply SHA256 hash) | プレーンテキストのメールアドレスを提供し、コネクタがこの値を空白トリムし、小文字に変換し、SHA256ハッシュを使用してハッシュ化します。 |
| Phone Number (already SHA256 hashed) | すでに空白をトリムし、小文字に変換し、SHA256でハッシュ化された電話番号を提供します。 |
| Phone Number (apply SHA256 hash) | プレーンテキストの電話番号を提供し、コネクタがこの値を空白トリムし、小文字に変換し、SHA256ハッシュを使用してハッシュ化します。 |
| First Name (already SHA256 hashed) | すでに空白をトリムし、小文字に変換し、SHA256でハッシュ化された名前を提供します。 |
| First Name (apply SHA256 hash) | プレーンテキストの名前を提供し、コネクタがこの値を空白トリムし、小文字に変換し、SHA256ハッシュを使用してハッシュ化します。 |
| Last Name (already SHA256 hashed) | すでに空白をトリムし、小文字に変換し、SHA256でハッシュ化された姓を提供します。 |
| Last Name (apply SHA256 hash) | プレーンテキストの姓を提供し、コネクタがこの値を空白トリムし、小文字に変換し、SHA256ハッシュを使用してハッシュ化します。 |
| Address (already SHA256 hashed) | すでに空白をトリムし、小文字に変換し、SHA256でハッシュ化された住所を提供します。 |
| Address (apply SHA256 hash) | プレーンテキストの住所を提供し、コネクタがこの値を空白トリムし、小文字に変換し、SHA256ハッシュを使用してハッシュ化します。 |
| City (already SHA256 hashed) | すでに空白をトリムし、小文字に変換し、SHA256でハッシュ化された市を提供します。 |
| City (apply SHA256 hash) | プレーンテキストの市を提供し、コネクタがこの値を空白トリムし、小文字に変換し、SHA256ハッシュを使用してハッシュ化します。 |
| State (already SHA256 hashed) | すでに空白をトリムし、小文字に変換し、SHA256でハッシュ化された州を提供します。 |
| State (apply SHA256 hash) | プレーンテキストの州を提供し、コネクタがこの値を空白トリムし、小文字に変換し、SHA256ハッシュを使用してハッシュ化します。 |
| Postal Code (already SHA256 hashed) | すでに空白をトリムし、小文字に変換し、SHA256でハッシュ化された郵便番号を提供します。 |
| Postal Code (apply SHA256 hash) | プレーンテキストの郵便番号を提供し、コネクタがこの値を空白トリムし、小文字に変換し、SHA256ハッシュを使用してハッシュ化します。 |
| MAID | モバイル広告ID。 |
| LiveRamp ID | LiveRamp ID。 |
