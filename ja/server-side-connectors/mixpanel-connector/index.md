---
title: Mixpanelコネクタ構成ガイド
description: この記事では、Mixpanelコネクタの構成方法について説明します。
url: https://docs.tealium.com/ja/server-side-connectors/mixpanel-connector/
---
## コネクタのアクション

| アクション名 | AudienceStream | EventStream |
| --- | :---: | :---: |
|イベントの追跡| ✓| ✓|
|プロフィールの更新 - セット| ✓| ✓|
|プロフィールの更新 - 追加| ✓| ✓|
|プロフィールの更新 - 追記| ✓| ✓|
|プロフィールの更新 - ユニオン| ✓| ✓|
|プロフィールの更新 - アンセット| ✓| ✓|
|プロフィールの更新 - 削除| ✓| ✓|
|プロフィールの更新 - 収益の追跡| ✓| ✓|
|エイリアスの作成| ✓| ✓|

## 構成の構成

コネクタマーケットプレイスに移動し、新しいコネクタを追加します。コネクタの追加方法の一般的な指示については、[コネクタの概要](https://docs.tealium.com/about-connectors/)の記事を参照してください。

コネクタを追加した後、以下の構成を構成します：

* **APIプロジェクトトークン**：プロフィールとイベントデータをMixpanelに送信するためには、プロジェクトトークンが必要です。詳細については、[プロジェクトトークンはどこで見つけることができますか？](https://mixpanel.com/help/questions/articles/where-can-i-find-my-project-token)を参照してください。
* **Mixpanel APIベースURL**：必須。Mixpanel APIベースURLを選択します。詳細については、[Mixpanel：プライバシープログラム](https://mixpanel.com/legal/eu-data-residency/)を参照してください。
  * 標準サーバー: `api.mixpanel.com`
  * EU居住者サーバー: `api-eu.mixpanel.com`
  * インド居住者サーバー: `api-in.mixpanel.com`

## アクション構成 - パラメータとオプション

**次へ**をクリックするか、**アクション**タブに移動します。ここでコネクタアクションを構成します。

このセクションでは、各アクションのパラメータとオプションの構成方法について説明します。

### アクション - イベントの追跡

#### パラメータ

|**パラメータ**| **説明**|
|---| ---|
|ユーザーID| イベントに関連付けられたユーザーの一意の識別子。|
|IPアドレス| ユーザーのIPアドレス。 |
|イベント時間の上書き| 推奨されません。自動的なイベントタイムスタンプをカスタム値（エポックタイム形式、UTC）で上書きします。 |
|イベント名| 必須。イベントの名前。 |
|イベントデータ|  <ul><li>イベントに追加するカスタムプロパティ。</li><li>日付型のプロパティはMixpanelの日付形式`YYYY-MM-DDThh:mm:ss`に変換されます。</li><li>空の値を持つプロパティはスキップされ、含まれません。</li></ul> |

### アクション - プロフィールの更新 - セット

#### パラメータ

|**パラメータ**| **説明**|
|---| ---|
|最終閲覧更新の無視| オプション。`true`に構成すると、Mixpanelは自動的にユーザープロフィールの"最終閲覧"時間プロパティを更新しません。 |
|ユーザーID| 必須。イベントに関連付けられたユーザーの一意の識別子。 |
|IPアドレス| ユーザーのIPアドレス。 |
|更新時間の上書き| 推奨されません。自動的なイベントタイムスタンプをカスタム値（エポックタイム形式、UTC）で上書きします。 |
|作成日| プロフィールが更新された日時。日付型のプロパティはMixpanelの日付形式`YYYY-MM-DDThh:mm:ss`に変換されます。 |
|電話番号| ユーザーの電話番号。|
|姓| ユーザーの姓。|
|メール| ユーザーのメールアドレス。 |
|名| ユーザーの名。|
|フルネーム| ユーザーのフルネーム。|
|既存のプロパティ値の上書きを防ぐ (一度だけ構成)| このチェックボックスを選択すると、コネクタが既存の値を上書きするのを防ぎます。 |

### アクション - プロフィールの更新 - 追加

#### パラメータ

|**パラメータ**| **説明**|
|---| ---|
|最終閲覧更新の無視|オプション。`true`に構成すると、Mixpanelは自動的にユーザープロフィールの"最終閲覧"時間プロパティを更新しません。 |
|ユーザーID| 必須。イベントに関連付けられたユーザーの一意の識別子。 |
|IPアドレス| ユーザーのIPアドレス。 |
|更新時間の上書き| 推奨されません。自動的なイベントタイムスタンプをカスタム値（エポックタイム形式、UTC）で上書きします。 |
|データの追加|  <ul><li>数値を指定して、既存の値に数値の量を追加します。</li><li>負の量を提供することで値を減らします。</li><li>空の値を持つプロパティはスキップされ、含まれません。</li></ul> |

### アクション - プロフィールの更新 - 追記

#### パラメータ

|**パラメータ**| **説明**|
|---| ---|
|最終閲覧更新の無視| オプション。`true`に構成すると、Mixpanelは自動的にユーザープロフィールの"最終閲覧"時間プロパティを更新しません。|
|ユーザーID| 必須。イベントに関連付けられたユーザーの一意の識別子。 |
|IPアドレス| ユーザーのIPアドレス。 |
|更新時間の上書き| 推奨されません。自動的なイベントタイムスタンプをカスタム値（エポックタイム形式、UTC）で上書きします。 |
|データの追記|  <ul><li>既存の値のリストに値を追加するプロパティを指定します。</li><li>存在しないプロパティに追加すると、1つの要素を持つ新しいリストが生成されます。</li><li>空の値を持つプロパティはスキップされ、含まれません。</li></ul> |

### アクション - プロフィールの更新 - ユニオン

#### パラメータ

|**パラメータ**| **説明**|
|---| ---|
|最終閲覧更新の無視| オプション。`true`に構成すると、Mixpanelは自動的にユーザープロフィールの"最終閲覧"時間プロパティを更新しません。|
|ユーザーID| 必須。イベントに関連付けられたユーザーの一意の識別子。 |
|IPアドレス| ユーザーのIPアドレス。 |
|更新時間の上書き| 推奨されません。自動的なイベントタイムスタンプをカスタム値（エポックタイム形式、UTC）で上書きします。 |
|ユニオンデータ|  <ul><li>ユーザープロフィール上の既存のリストとその値をマージするプロパティを指定します。</li><li>プロパティはデータレイヤー内で配列型でなければなりません。</li><li>空の値を持つプロパティはスキップされ、含まれません。</li><li>重複する値は無視されます。</li></ul> |

### アクション - プロフィールの更新 - アンセット

#### パラメータ

|**パラメータ**| **説明**|
|---| ---|
|最終閲覧更新の無視|オプション。`true`に構成すると、Mixpanelは自動的にユーザープロフィールの"最終閲覧"時間プロパティを更新しません。|
|ユーザーID| 必須。イベントに関連付けられたユーザーの一意の識別子。 |
|IPアドレス| ユーザーのIPアドレス。 |
|更新時間の上書き| 推奨されません。自動的なイベントタイムスタンプをカスタム値（エポックタイム形式、UTC）で上書きします。 |
|アンセットデータ|  <ul><li>ユーザープロフィールから永久に削除するためにプロパティとその値を指定します。</li><li>データレイヤーで配列型をサポートします。</li></ul> |

### アクション - プロフィールの更新 - 削除

#### パラメータ

|**パラメータ**| **説明**|
|---| ---|
|最終閲覧更新の無視| オプション。`true`に構成すると、Mixpanelは自動的にユーザープロフィールの"最終閲覧"時間プロパティを更新しません。|
|ユーザーID| 必須。イベントに関連付けられたユーザーの一意の識別子。 |
|IPアドレス| ユーザーのIPアドレス。 |
|更新時間の上書き| 推奨されません。自動的なイベントタイムスタンプをカスタム値（エポックタイム形式、UTC）で上書きします。 |

### アクション - プロフィールの更新 - 収益の追跡

#### パラメータ

|**パラメータ**| **説明**|
|---| ---|
|最終閲覧更新の無視|オプション。`true`に構成すると、Mixpanelは自動的にユーザープロフィールの"最終閲覧"時間プロパティを更新しません。|
|ユーザーID| 必須。イベントに関連付けられたユーザーの一意の識別子。 |
|IPアドレス| ユーザーのIPアドレス。 |
|更新時間の上書き| 推奨されません。自動的なイベントタイムスタンプをカスタム値（エポックタイム形式、UTC）で上書きします。 |
|取引金額| 必須。取引の金額。|
|取引時間| 必須。取引の時間。日付型のプロパティはMixpanelの日付形式`YYYY-MM-DDThh:mm:ss`に変換されます。|

### アクション - エイリアスの作成

#### パラメータ

|**パラメータ**| **説明**|
|---| ---|
|元のユーザーID|  <ul><li>ユーザーに以前に構成された識別子。</li><li>通常は、より良いユーザーIDが利用可能になる前に一時的に生成されたID（例：サインアップ後のユーザーのメール）</li></ul> |
|新しいエイリアスユーザーID|  <ul><li>元のIDにマップする新しいID。</li><li>通常、エイリアスはユーザーがサインアップ直後に構成され、その後のすべてのイベントと更新は新しいエイリアスIDを使用します。</li><li>詳細については、[Mixpanel: Mixpanelエイリアスの使用](https://mixpanel.com/docs/integration-libraries/using-mixpanel-alias)を参照してください。</li></ul>. |
