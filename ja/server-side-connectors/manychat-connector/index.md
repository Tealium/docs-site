---
title: Manychatコネクタ構成ガイド
description: この記事では、Manychatコネクタの構成方法について説明します。
url: https://docs.tealium.com/ja/server-side-connectors/manychat-connector/
---
## API情報

このコネクタは以下のベンダーAPIを使用します：

* API名：Manychat API
* APIエンドポイント：`https://api.manychat.com`
* ドキュメント：[Manychat API](https://api.manychat.com/swagger#/)

## 構成

コネクタマーケットプレイスにアクセスし、新しいコネクタを追加します。コネクタを追加する一般的な手順については、[コネクタについて]()を参照してください。

コネクタを追加した後、以下の構成を構成します：

* **APIキー**  
  *   Manychatはアカウント公開APIで使用するAPIキーを提供します。

## アクション

| アクション名 | AudienceStream | EventStream |
| --- | :---: | :---: |
| 名前でタグを追加 | ✓ | ✗ |
| 名前でタグを削除 | ✓ | ✗ |
| 名前でカスタムフィールドを構成 | ✓ | ✗ |
| サブスクライバーをアップサート | ✓ | ✗ |
| メッセージを送信 | ✓ | ✗ |

アクションの名前を入力し、ドロップダウンメニューからアクションタイプを選択します。

次のセクションでは、各アクションのパラメータとオプションの構成方法について説明します。

### 名前でタグを追加

#### パラメータ

| **パラメータ** | **説明** |
| --- | --- |
| サブスクライバーID | サブスクライバーの一意の識別子。 |
| タグ名 | サブスクライバーに付けるラベル。 |

### 名前でタグを削除

#### パラメータ

| **パラメータ** | **説明** |
| --- | --- |
| サブスクライバーID | サブスクライバーの一意の識別子。 |
| タグ名 | サブスクライバーから削除するタグ。 |

### 名前でカスタムフィールドを構成

#### パラメータ

| **パラメータ** | **説明** |
| --- | --- |
| サブスクライバーID | サブスクライバーの一意の識別子。 |
| フィールド名 | 値を構成するフィールド名。 |
| フィールド値 | フィールド値。 |
| 日付フィールド | オプション。カスタムフィールドが日時形式(`YYY-MM-DDTHH:MM:SS&#43;00:00`)を期待する場合は、チェックボックスを選択します。それ以外の場合は、`YYYY-MM-DD`の日付形式が使用されます。 |

### サブスクライバーをアップサート

#### サブスクライバーパラメータ

| **パラメータ** | **説明** |
| --- | --- |
| サブスクライバーID | サブスクライバーの一意の識別子。 |
| 名 | サブスクライバーの名。 |
| 姓 | サブスクライバーの姓。 |
| 電話 | サブスクライバーの電話番号。 |
| メール | サブスクライバーのメールアドレス。 |
| Whatsapp電話 | サブスクライバーのWhatsApp電話番号。 |
| 性別 | サブスクライバーの性別。 |
| SMSオプトインあり | ブール値。電話番号プロパティが空でない場合に必要です。 |
| メールオプトインあり | ブール値。メールプロパティが空でない場合に必要です。 |
| 同意フレーズ | SMSオプトインが`true`の場合に必要です。 |

### メッセージを送信

#### メッセージパラメータ

| **パラメータ** | **説明** |
| --- | --- |
| サブスクライバーID | サブスクライバーの一意の識別子。 |
| データ | メッセージデータ。このオブジェクトを構築するためにはテンプレート変数を使用する必要があります。[詳細情報](https://manychat.github.io/dynamic_block_docs/)。 |
| メッセージタグ | メッセージを説明するタグ。例：`ACCOUNT_UPDATE`。 |
| OTNトピック名 | 一回限りの通知トピック名。例：チャンネルニュース。 |
| テンプレート変数 | **テンプレート**のデータ入力としてテンプレート変数を提供します。&lt;br&gt;詳細情報と使用例については、を参照してください。&lt;br&gt;ドット表記でネストされたテンプレート変数に名前を付けます。例：items.name。&lt;br&gt;ネストされたテンプレート変数は通常、データレイヤーリスト属性から構築されます。 |
| テンプレート | データフィールドで参照されるテンプレートを提供します。詳細については、を参照してください。&lt;br&gt;テンプレートは、例えば`{{SomeTemplateName}}`のように、サポートされるフィールドに名前でダブルカーリーブレースで注入されます。 |

