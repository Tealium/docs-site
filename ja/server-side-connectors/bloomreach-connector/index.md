---
title: Bloomreachコネクタ構成ガイド
description: この記事では、Bloomreachコネクタの構成方法について説明します。
url: https://docs.tealium.com/ja/server-side-connectors/bloomreach-connector/
---
## API情報

このコネクタは以下のベンダーAPIを使用します：

* API名：Bloomreach API
* APIバージョン：v2.0
* APIエンドポイント：`https://api-engagement.bloomreach.com`
* ドキュメント：[Bloomreach API](https://documentation.bloomreach.com/engagement/reference/about)

## 構成

コネクタマーケットプレイスにアクセスし、新しいコネクタを追加します。コネクタを追加する一般的な手順については、[コネクタについて]()を参照してください。

コネクタを追加した後、以下の構成を構成します：
* **APIキー**  
APIキー。
* **APIシークレット**  
APIシークレット。
* **プロジェクトトークン**  
あなたのプロジェクトトークン。これは、Engagementウェブアプリの**プロジェクト構成 &gt; アクセス管理 &gt; API**で見つけることができます。
* **APIドメイン**  
あなたのBloomreach APIドメイン。デフォルト値は `https://api-engagement.bloomreach.com` です。

## バッチ制限

このアクションは、ベンダーへの大量データ転送をサポートするためにバッチリクエストを使用します。詳細については、バッチアクションを参照してください。リクエストは、次のいずれかの閾値に達するか、プロファイルが公開されるまでキューに入れられます：

* 最大リクエスト数：50
* 最古のリクエストからの最大時間：5分
* リクエストの最大サイズ：30 MB

## アクション

| アクション名 | AudienceStream | EventStream |
| --- | :---: | :---: |
| 顧客プロパティの更新 | ✓ | ✗ |
| 顧客プロパティのバッチ更新 | ✓ | ✗ |
| イベントの追加 | ✓ | ✓ |
| イベントバッチの追加 | ✓ | ✓ |
| トランザクショナルメールのトリガー | ✓ | ✓ |
| トランザクショナルSMSのトリガー | ✓ | ✓ |

アクションの名前を入力し、ドロップダウンメニューからアクションタイプを選択します。

次のセクションでは、各アクションのパラメータとオプションの構成方法について説明します。

### 顧客プロパティの更新

#### 顧客ID

| **パラメータ** | **説明** |
| --- | --- |
| 登録済み | (必須) ハードまたはソフトID。 |

#### プロパティ

(必須) 更新する顧客プロパティ。

| **パラメータ** | **説明** |
| --- | --- |
| メール | 顧客のメールアドレス。 |
| 名 | 顧客の名前。 |
| 姓 | 顧客の姓。 |

#### オプショナル

| **パラメータ** | **説明** |
| --- | --- |
| 更新タイムスタンプ | UNIXタイムスタンプ（秒）。更新時間が古いプロパティのみが更新されます。 |

### 顧客プロパティのバッチ更新

#### 顧客ID

| **パラメータ** | **説明** |
| --- | --- |
| 登録済み | (必須) ハードまたはソフトID。 |

#### プロパティ

(必須) 更新する顧客プロパティ。

| **パラメータ** | **説明** |
| --- | --- |
| メール | 顧客のメールアドレス。 |
| 名 | 顧客の名前。 |
| 姓 | 顧客の姓。 |

#### オプショナル

| **パラメータ** | **説明** |
| --- | --- |
| 更新タイムスタンプ | UNIXタイムスタンプ（秒）。更新時間が古いプロパティのみが更新されます。 |

### イベントの追加

#### パラメータ

| **パラメータ** | **説明** |
| --- | --- |
| イベントタイプ | イベントのタイプ。選択されたプロパティカテゴリに基づいて必要なだけ多くのイベントをトリガーするイベントタイプ。&lt;ul&gt;&lt;li&gt;購入&lt;/li&gt;&lt;li&gt;購入アイテム&lt;/li&gt;&lt;li&gt;カート更新&lt;/li&gt;&lt;li&gt;チェックアウト&lt;/li&gt;&lt;li&gt;アイテム閲覧&lt;/li&gt;&lt;li&gt;カテゴリ閲覧&lt;/li&gt;&lt;li&gt;同意&lt;/li&gt;&lt;li&gt;バナー&lt;/li&gt;&lt;li&gt;推奨&lt;/li&gt;&lt;/ul&gt; |

#### 顧客ID

| **パラメータ** | **説明** |
| --- | --- |
| 登録済み | (必須) ハードまたはソフトID。 |

#### プロパティ

| **パラメータ** | **説明** |
| --- | --- |
| タイムスタンプ | イベントが作成されたときのUNIXタイムスタンプ（秒）。提供されていない場合は、現在の時間が使用されます。タイムスタンプはISO 8601形式の日時をサポートします。値は秒単位である必要があります。文字数に基づいてミリ秒が自動的に認識されることはありません。 |

### イベントバッチの追加

#### パラメータ

| **パラメータ** | **説明** |
| --- | --- |
| イベントタイプ | イベントのタイプ。選択されたプロパティカテゴリに基づいて必要なだけ多くのイベントをトリガーするイベントタイプ。&lt;ul&gt;&lt;li&gt;購入&lt;/li&gt;&lt;li&gt;購入アイテム&lt;/li&gt;&lt;li&gt;カート更新&lt;/li&gt;&lt;li&gt;チェックアウト&lt;/li&gt;&lt;li&gt;アイテム閲覧&lt;/li&gt;&lt;li&gt;カテゴリ閲覧&lt;/li&gt;&lt;li&gt;同意&lt;/li&gt;&lt;li&gt;バナー&lt;/li&gt;&lt;li&gt;推奨&lt;/li&gt;&lt;/ul&gt; |

#### 顧客ID

| **パラメータ** | **説明** |
| --- | --- |
| 登録済み | (必須) ハードまたはソフトID。 |

#### プロパティ

| **パラメータ** | **説明** |
| --- | --- |
| タイムスタンプ | イベントが作成されたときのUNIXタイムスタンプ（秒）。提供されていない場合は、現在の時間が使用されます。タイムスタンプはISO 8601形式の日時をサポートします。値は秒単位である必要があります。文字数に基づいてミリ秒が自動的に認識されることはありません。 |

### トランザクショナルメールのトリガー

#### パラメータ

| **パラメータ** | **説明** |
| --- | --- |
| インテグレーションID | メールサービスプロバイダーインテグレーションのID。これは、選択されたインテグレーションのURLで見つけることができます。 |

#### インテグレーション

| **パラメータ** | **説明** |
| --- | --- |
| ID | (必須) メールサービスプロバイダーインテグレーションのID。 |
| 送信者アドレス | このインテグレーションのメールの送信者として使用されるメールアドレス。他の方法でこのパラメータを提供することを上書きします。 |

#### メール内容

| **パラメータ** | **説明** |
| --- | --- |
| テンプレートID | 送信したいメールテンプレートのID。これは、選択された事前定義されたテンプレートのURLで見つけることができます。 |
| 件名 | メールの件名を定義します。このフィールドではJinjaは評価されません。 |
| 送信者アドレス | 送信者のメールアドレス。 |
| 送信者名 | 通常、送信者の名前です。 |
| 添付ファイルのファイル名 | 添付ファイルの名前。 |
| 添付ファイルの内容 | ファイルのBase64エンコードされた内容。 |
| 添付ファイルのコンテンツタイプ | 添付ファイルの形式。 |
| Html | `&lt;html&gt;` と `&lt;/html&gt;` タグの間のメールの内容を定義します。CSSコンテンツを含めることができます。Jinjaはこのフィールドでは評価されません。 |
| メール内容パラメータ | テンプレートで使用されるメールのパーソナライゼーションに必要なすべてのデータを提供します。形式：`param_name`:&#34;value&#34;。例：`first_name`:&#34;Marian&#34; |
| キャンペーン名 | メールキャンペーンの名前。 |

#### 受信者

| **パラメータ** | **説明** |
| --- | --- |
| メール | 顧客のメールを指定します。メールが提供されていない場合、顧客IDに基づいてBloomreach Engagementの顧客プロファイルから取得されます。 |
| 言語 | テンプレートの言語バージョン。テンプレートに複数の言語バージョンが作成されている場合、このメールに使用するバージョンを選択できます。 |

#### 受信者顧客ID

| **パラメータ** | **説明** |
| --- | --- |
| 転送アイデンティティ | リンク転送機能を有効にするかどうか。可能な値：&lt;ul&gt;&lt;li&gt;`Enabled` - デフォルト値、すべてのクリックで識別する`iitt`パラメータを追加します。&lt;/li&gt;&lt;li&gt;`Disabled` - リンクに何も追加せず、アイデンティティ転送はありません。&lt;/li&gt;&lt;li&gt;`First Click` - 最初のクリックのみを識別する`xnpe_tifc`を追加します。&lt;/li&gt;&lt;/ul&gt; |

#### メールテンプレート構成

| **パラメータ** | **説明** |
| --- | --- |
| ユーザーアイデンティティの転送 | リンク転送機能を有効にするかどうか。可能な値：&lt;ul&gt;&lt;li&gt;`enabled` (デフォルト値)&lt;/li&gt;&lt;li&gt;`disabled`&lt;/li&gt;&lt;li&gt;`iitt` - リンクにアイデンティティを転送しない。&lt;/li&gt;&lt;/ul&gt; この構成は、リクエストの本文に直接指定される`transferidentity`パラメータと同じです。どちらか一方のみを指定する必要があります。構成の一部として転送アイデンティティを指定し、単独で指定するとエラーが発生します。 |
| 同意カテゴリ | Engagementプラットフォームでの同意を尊重するために、顧客の同意カテゴリを指定します。 |
| トラッキング同意カテゴリ | Engagementプラットフォームでのトラッキング同意を尊重するために、顧客のトラッキング同意カテゴリを指定します。 |
| メールテンプレートカスタムイベントプロパティ | カスタム属性。 |
| メールテンプレートカスタムヘッダー | メールプロバイダに送信するリクエストヘッダ。 |
| メールテンプレートURLパラメータ | セッション開始イベントでのトラッキングに使用されるURLパラメータ。 |

### トランザクショナルSMSのトリガー


#### SmsContent

| **パラメータ** | **説明** |
| --- | --- |
| テンプレートID | 送信するSMSテンプレートのID。選択した事前定義テンプレートのURLで見つけることができます。 |
| メッセージ | SMSメッセージの内容。 |
| 最大メッセージ部分数 | メッセージ部分の最大許容数。これを超えると、SMSメッセージは送信されません。最小値: `1`、最大値: `8`、デフォルト値: `8`。 |
| 送信者 | あなたの送信者電話番号。 |
| Smsコンテンツパラメータ | テンプレートで使用されるSMSのパーソナライゼーションに必要なすべてのデータを提供します。形式: `param_name`:&#34;value&#34;。例: `first_name`:&#34;Marian&#34;。 |
| キャンペーン名 | SMSキャンペーンの名前。 |

#### 受信者

| **パラメータ** | **説明** |
| --- | --- |
| 電話 | 顧客の電話番号を指定します。電話番号が提供されていない場合、顧客IDに基づいてBloomreach Engagementの顧客プロファイルから取得されます。 |
| 言語 | テンプレートの言語バージョン。テンプレートに複数の言語バージョンが作成されている場合、このSMSに使用するバージョンを選択できます。 |

#### 受信者顧客ID

| **パラメータ** | **説明** |
| --- | --- |

#### SMSテンプレート構成

| **パラメータ** | **説明** |
| --- | --- |
| ユーザーIDの転送 | リンク転送機能を有効にするかどうか。可能な値:&lt;ul&gt;&lt;li&gt;`enabled` (デフォルト値)&lt;/li&gt;&lt;li&gt;`disabled`&lt;/li&gt;&lt;li&gt;`iitt` - リンクにIDを転送しない。&lt;/li&gt;&lt;/ul&gt; この構成は、リクエストの本文の `transferidentity` パラメータと同じです。どちらか一方のみを指定する必要があります。構成と単独でのID転送の両方を指定するとエラーになります。 |
| 同意カテゴリ | Engagementプラットフォームでの同意を尊重するために、顧客の同意カテゴリを指定します。 |
| トラッキング同意カテゴリ | Engagementプラットフォームでのトラッキング同意を尊重するために、顧客のトラッキング同意カテゴリを指定します。 |
| SMSテンプレートカスタムイベントプロパティ | カスタム属性。 |
| SMSテンプレートカスタムヘッダー | SMSプロバイダーに送信するリクエストヘッダー。 |
| SMSテンプレートURLパラメータ | セッション開始イベントのトラッキングに使用されるURLパラメータ。 |